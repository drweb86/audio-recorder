<Page
    x:Class="HDE.AudioRecorder.Views.RecordingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Loaded="Page_Loaded">

    <Grid Width="Auto" Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>

            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.Resources>
            <Style TargetType="HyperlinkButton">
                <Setter Property="Margin" Value="10 5 10 0"/>
            </Style>
            <Style TargetType="Button">
                <Setter Property="Margin" Value="20 5 10 0"/>
                <Setter Property="CornerRadius" Value="5"/>
            </Style>
        </Grid.Resources>

        <Button Grid.Row="0" Click="OnRecordClick" Style="{StaticResource AccentButtonStyle}" Margin="10">
            <StackPanel Orientation="Horizontal">
                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="{Binding ButtonGlyph}" Margin="5" />
                <TextBlock Text="{Binding ButtonTitle}" Margin="5"></TextBlock>
            </StackPanel>
        </Button>
        <HyperlinkButton Grid.Row="1" Click="OnOpenRecordingsFolder" x:Uid="seeRecordings"></HyperlinkButton>
    </Grid>
</Page>
