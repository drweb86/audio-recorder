<Page
    x:Class="HDE.AudioRecorder.Views.ApplicationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid Margin="15 15 0 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.Style>
            <Style TargetType="HyperlinkButton">
                <Setter Property="Margin" Value="10 0 0 0"></Setter>
                <Setter Property="VerticalAlignment" Value="Center"></Setter>
            </Style>
        </Grid.Style>

        <HyperlinkButton x:Uid="LogsFolder" Grid.Row="0" Click="OnOpenLogsFolder"></HyperlinkButton>
        <HyperlinkButton x:Uid="SupportLink" Grid.Row="1" Click="OnOpenSupportLink"></HyperlinkButton>
        <HyperlinkButton x:Uid="LicenseLink" Grid.Row="2" Click="OnOpenLicenseLink"></HyperlinkButton>
        <HyperlinkButton x:Uid="PrivatePolicyLink" Grid.Row="3" Click="OnOpenPrivatePolicyLink"></HyperlinkButton>
    </Grid>
</Page>
