<Page
    x:Class="HDE.AudioRecorder.Views.RecordingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HDE.AudioRecorder.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Width="Auto" Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>

            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.Resources>
            <Style TargetType="HyperlinkButton">
                <Setter Property="Margin" Value="10 5 10 0"/>
            </Style>
            <Style TargetType="Button">
                <Setter Property="Margin" Value="10 5 10 0"/>
            </Style>
        </Grid.Resources>

        <Button Grid.Row="0" Click="OnRecordClick" Content="{Binding ButtonTitle}" />
        <HyperlinkButton Grid.Row="1" Click="OnOpenRecordingsFolder">see recordings</HyperlinkButton>
    </Grid>
</Page>
